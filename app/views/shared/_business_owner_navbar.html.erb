<nav class="navbar navbar-expand-lg bg-body-tertiary mb-3">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">BookMe</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Home", businesses_path, class: "nav-link active", aria: { current: "page" } %>
          </li>
          <li class="nav-item dropdown">
            <%= content_tag(:a, "Bookings", class: "nav-link active dropdown-toggle", href: "#", role: "button", data: { bs_toggle: "dropdown" }, aria: { expanded: "false" }) %>
            <ul class="dropdown-menu">
              <li><%= link_to "Your Bookings", client_bookings_path(current_user.username), class: "dropdown-item" %></li>
              <% current_user.businesses.each do |business| %>
                <li>
                  <%= link_to "#{business.name} Bookings", business_bookings_path(business.name), class: "dropdown-item" %>
                </li>
              <% end %>
            </ul>
            <li class="nav-item">
              <%= link_to "Services", services_path, class: "nav-link active" %>
            </li>
            <li class="nav-item">
              <%= link_to "Business Hours", business_hours_path, class: "nav-link active" %>
            </li>
          </ul>
          <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            <li><%= link_to "Edit profile", edit_user_registration_path, class: "nav-link active" %></li>
            <li><%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-link active" %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
