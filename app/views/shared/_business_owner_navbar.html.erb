<nav class="navbar navbar-expand-lg bg-body-tertiary mb-3">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">BookMe</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/businesses/">Home</a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Bookings
            </a>
            <ul class="dropdown-menu">

              <li><%= link_to "Your Bookings", client_bookings_path(current_user.username), class: "dropdown-item" %></li>
              <!-- TODO: address when a user has multiple businesses OR when a user is not a business owner -->
              <li><%= link_to "#{current_user.businesses.first.name} Bookings", business_bookings_path(current_user.businesses.first.name), class: "dropdown-item" %></li>
            </ul>
            <li class="nav-item">
              <a class="nav-link" href="/services/">Services</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/business_hours/">Business Hours</a>
            </li>
          </ul>
          <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            <li><%= link_to "Edit profile", edit_user_registration_path, class: "nav-link active" %></li>
            <li><%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-link active" %></li>

          <% end %>
        </ul>
      </div>
    </div>
  </nav>
