<div class="d-flex justify-content-center align-items-center">
  <div class="col-md-6">
    <h1>Editing booking</h1>

    <%= render "form", booking: @booking %>

    <br>

    <div>
      <%= link_to "Back to bookings", business_bookings_path(@booking.business.name), class: "btn btn-secondary" %>
    </div>
  </div>
</div>
