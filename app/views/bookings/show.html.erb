<%= render @booking %>

<div>
  <%= link_to "Edit this booking", edit_booking_path(@booking) %> |
  <%= link_to "Back to bookings", bookings_path %>

  <%= button_to "Cancel this booking", booking_path(@booking.id), method: :delete, class: "btn btn-danger" %>
</div>
