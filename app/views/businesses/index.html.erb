<div class="d-flex flex-column justify-content-center align-items-center">
  <h1>Welcome to <strong>BookMe!</strong></h1>
  <%= image_tag asset_path('favicon.png'), width: "100", height: "100", class: "mb-3" %>
  <p><%= render "search/alternate_button" %> for businesses to book an appointment, or <%= link_to "add your business", new_business_path %> to start managing your bookings!</p>
</div>

<div class="text-center">
  <h2>Businesses in your area</h2>
</div>

<div class="container mt-4">
  <div class="masonry-grid">
    <% @businesses.each do |business| %>
      <div class="masonry-item">
        <%= render "businesses/card", business: business %>
      </div>
    <% end %>
  </div>
</div>
